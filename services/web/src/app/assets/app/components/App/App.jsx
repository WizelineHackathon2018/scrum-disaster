/* global React */

import ProgressBar from '../ProgressBar';

export default class App extends React.Component {
  render() {
    return (
      <div>
        <ProgressBar />
      </div>
    );
  }
}
